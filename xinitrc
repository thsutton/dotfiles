#!/bin/sh

if [ -d /etc/X11/xinit/xinitrc.d ]; then
  for f in /etc/X11/xinit/xinitrc.d/*; do
    [ -x "$f" ] && source "$f"
  done
  unset f
fi

cabal --sandbox-config-file=$HOME/.xmonad/cabal.sandbox.config exec xmonad 
